export type PropsTypeProducts = {
  id: number;
  title: string;
  price: number;
  desc: string;
  size?: string | number | null;
};

export type PropsTypeUser = {
  name: string;
  email: string;
};
